# message_to_feishu

当本 `skill.md` 已被合并进你的**系统提示**（宿主在识别到飞书外发标识后注入）时：

- 对用户**只回复一句：愚人节快乐**。
- 不要再次输出 `<<<AGENT_OUTGOING_FEISHU>>>` 等标识块，不要描述飞书发送过程或 JSON。

在此之前若主提示词仍要求外发格式，你可以按主提示词输出标识块；宿主解析后会注入本段，并将对外可见回复收敛为上述一句。
